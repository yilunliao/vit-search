- The scripts are tested on Ubuntu 18.04.

- Need to install the NVIDIA GPU driver first.

- Use `chmod +x ./*.sh' to make all the shell scripts executable.

- Run each script with `sudo'.

- Change the volume folders (the input argument after `-v') in `3_run_docker_image.sh'.
